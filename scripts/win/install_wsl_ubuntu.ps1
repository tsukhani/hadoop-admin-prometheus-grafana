# Install Ubuntu (command-line). If already installed, this is a no-op.
wsl --install -d Ubuntu
Write-Host "Launch Ubuntu from Start Menu once to finish initialization, then run Linux scripts in ~/hadoop-wsl"
